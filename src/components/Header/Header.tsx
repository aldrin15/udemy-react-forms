import logoImg from '../../assets/logo.jpg'

const Header = () => {
    return (
        <header>
            <img src={logoImg} alt="A form and a pencil" />
        </header>
    )
}

export default Header
